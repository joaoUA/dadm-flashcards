<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginVertical="5dp"
    android:paddingHorizontal="10dp">

    <TextView
        android:id="@+id/tvItemDeckName"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:minHeight="50dp"
        android:text="@string/app_name"
        android:gravity="center_vertical"
        android:paddingHorizontal="10dp"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:background="#4477ff"
        />
    <LinearLayout
        android:id="@+id/llItemDeckActions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvItemDeckName"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        android:background="@color/black"
        android:visibility="gone"
        >
        <Button
            android:id="@+id/btnEditItemDeck"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/editDeck"
            android:layout_marginHorizontal="5dp"/>
        <Button
            android:id="@+id/btnStudyItemDeck"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/studyDeck"
            android:layout_marginHorizontal="5dp"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>